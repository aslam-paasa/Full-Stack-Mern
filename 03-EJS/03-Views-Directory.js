/**
 * Views Directory:
 * => const path = require('path');
 * => app.set("views", path.join(__dirname, "/views"));
 * 
 * => path.join() is used to join two paths.
 * => __dirname : server means jaha pe humne code likhe h ye uski directory h.
 * => "views" is ejs path.
 * => Using "views" we are joining __dirname + "./views" 
 * */ 