/**
 * Interpolation Syntax:
 * => Iterpolation refers to embedding expressions into marked up text.
 * => Means HTML k andr hm kuch kuch expressions daalnge jinki values baad
 *    m evaluate hogi.
 * => Example: `This is a ${name}` => variable embedding means iske andr
 *              ek variable ko add kr diye.
 * => Similarly, EJS k andr hum poore k poore JS Code ko embedd kr skte h.
 *  
 * Q. Kaise hm JS Expression ko EJS k andr embedd krnge?
 * => Using EJS, we will make our HTML dynamic.
 * */ 